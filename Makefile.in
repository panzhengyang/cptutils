# Makefile.in for gimpcpt

# $Id: Makefile.in,v 1.1 2002/06/18 21:58:20 jjg Exp jjg $
# help older versions make

@SET_MAKE@

# Macros

VERSION   = @VERSION@
RUBBISH   = *~
CONFFILES = config.cache config.log config.status configure Makefile

DIST      = gimpcpt-$(VERSION)

# targets

default : all

all : 
	cd src ; $(MAKE) all ; cd ..

install : all
	cd src ; $(MAKE) install ; cd ..

check test show : all
	cd src ; $(MAKE) test ; cd ..

clean :
	$(RM) $(RUBBISH)
	cd src ; $(MAKE) clean ; cd ..

veryclean : clean
	$(RM) $(CONFFILES)
	autoconf
	cd src ; $(MAKE) veryclean ; cd ..

dist : veryclean
	cd .. ; tar --exclude=RCS -zcvf $(DIST).tar.gz gimpcpt ; cd gimpcpt

maint-dist : clean
	cd .. ; tar -zcvf $(DIST)-maintain.tar.gz gimpcpt ; cd gimpcpt



