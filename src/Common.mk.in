# -*- makefile -*-
# Commom Makefile macros
# $Id: Common.mk.in,v 1.8 2007/11/16 22:13:30 jjg Exp jjg $ 

CC       = @CC@
CFLAGS  += @CFLAGS@ @DEFS@
LDFLAGS += 
LDLIBS  += @LIBS@

INSTALL         = @INSTALL@
INSTALL_PROGRAM = @INSTALL_PROGRAM@ 
INSTALL_DATA    = @INSTALL_DATA@ 
INSTALL_DIR     = @INSTALL@ -d 
GGO             = @GGO@
XML2C		= @XML2C@
GPERF		= @GPERF@

prefix      = @prefix@
exec_prefix = @exec_prefix@
bindir      = @bindir@
mandir      = @mandir@
datarootdir = @datarootdir@

RUBBISH += *~

# useful, but not needed for the build

ifneq (no,$(GGO))
%.h %.c : %.ggo
	$(GGO) -u -i $< -f $* -F $*
endif

