language: c
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libxml2-dev libpng12-dev
  - autoconf
  - autoheader
install:
  - ./configure
  - make version
  - make install
